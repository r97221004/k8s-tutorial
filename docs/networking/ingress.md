# Ingress

> One entry point that routes outside traffic to many Services by host and path.

---

_TODO — vanilla kubeadm has no built-in ingress controller; install ingress-nginx, then route by host/path. Ingress vs Service of type LoadBalancer; a rule that maps `/` and `/api` to two Services._

---

[← Service Discovery & DNS](dns.md) · [↑ Contents](../../README.md) · [Helm (intro) →](../packaging/helm.md)
