apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
spec:
  accessModes:
    - ReadWriteOnce        # mounted read-write by a single node
  resources:
    requests:
      storage: 1Gi
  # storageClassName: local-path   # uncomment if your cluster needs an explicit class
