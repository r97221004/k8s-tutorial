apiVersion: v1
kind: Pod
metadata:
  name: crasher
  labels:
    app: crasher
spec:
  containers:
    - name: crasher
      image: busybox:1.36
      # exits 1 after a second → Kubernetes restarts it → CrashLoopBackOff
      command: ["sh", "-c", "echo starting; sleep 1; echo 'boom' >&2; exit 1"]
